import React from "react";
import { PredictionCard } from "./components/PredictionCard";

const App: React.FC = () => {
  return (
    <div style={{ padding: 40 }}>
      <PredictionCard />
    </div>
  );
};

export default App;
